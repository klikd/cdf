
  # CDF Whistleblowing Prototype

  This is a code bundle for CDF Whistleblowing Prototype. The original project is available at https://www.figma.com/design/kam8BI05hx1DENHQ6ZkjCX/CDF-Whistleblowing-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  